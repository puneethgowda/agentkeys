[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agentkeys"
version = "0.1.0"
description = "Python SDK for AgentKeys — API key manager for AI agents"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.9"
dependencies = ["httpx>=0.25.0"]

[project.urls]
Homepage = "https://github.com/aravat90/agentkeys"
Documentation = "https://github.com/aravat90/agentkeys/tree/main/docs"
